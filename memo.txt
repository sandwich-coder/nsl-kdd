1.   An instance attribute always starts with an underbar. It is referenced, assigned, and removed only in functions defined in its class, viewed as a small namespace in which objects are internally shared.
2.   'if' statements follow the "false-first".
